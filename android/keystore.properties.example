## Copy this file to `android/keystore.properties` (do NOT commit the real one).
##
## `storeFile` is resolved relative to the `android/` folder.
## Example (keystore stored in repo root `backend/` folder):
## storeFile=../backend/billsafe-release.jks
##
storeFile=../backend/billsafe-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=YOUR_KEY_ALIAS
keyPassword=YOUR_KEY_PASSWORD
